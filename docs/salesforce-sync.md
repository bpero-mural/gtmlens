# Salesforce Sync

Salesforce OAuth and sync are not implemented in Stage 0.

## Stage 0 Boundary

- No Salesforce OAuth controller.
- No Salesforce API client.
- No metadata collectors.
- No sync command.
- No business record queries.

The database schema includes empty foundation tables so Stage 1 can add connection and sync behavior without replacing the local Docker foundation.
